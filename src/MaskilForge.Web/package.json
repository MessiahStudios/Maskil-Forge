{
  "name": "maskil-forge-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:soundfont-worklet": "node scripts/sync-soundfont-worklet.mjs",
    "predev": "npm run sync:soundfont-worklet",
    "dev": "vite",
    "test": "node --test src/*.test.mjs",
    "prebuild": "npm run sync:soundfont-worklet",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "spessasynth_core": "^4.3.22",
    "spessasynth_lib": "^4.3.14",
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "typescript": "~5.9.3",
    "vite": "^7.1.7",
    "vue-tsc": "^3.1.0"
  }
}
